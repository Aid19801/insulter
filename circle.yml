machine:
  node:
    version: 6.3.0

dependencies:
  override:
    - echo =yoyoyoyo=

deployment:
  production:
    branch: master
    commands:
      - ./circleCI/deploy_prod.sh

  staging:
    branch: master
    commands:
      - ./circleCI/deploy_prod.sh

general:
  artifacts:
    - /home/ubuntu/insulter/android/app/build/outputs/apk/release/app-release.apk
